<ul class="post-list js-post-list">
{% for post in collections.blog.slice(0, max) %}

  {% set url = post.url %}
  {% set publication = post.data.publication %}
  {% set theme = publication %}

  {% if post.data.css %}
    {% set theme = post.data.css %}
  {% endif %}

  {% if not post.data.draft %}
    <li class="post-list__item post-list__item--{{ theme | stringify | slug }}">
      <a href="{{ url }}" class="post-list__link post-list__link--{{ theme | stringify | slug }}" data-post="{{ post.data.title | slug }}" data-publication="{{ theme | slug }}">
        <h2 class="post-list__heading post-list__heading--{{ theme | stringify | slug }}">
          <span class="post-list__heading-inner post-list__heading-inner--{{ theme | stringify | slug }}">
            {{ post.data.title }}
          </span>
        </h2>
      </a>

      {% for pub in collections.publications %}
        {% if publication == pub.data.title %}
          <div class="post-list__image post-list__image--{{ theme | stringify | slug }}">
            <img class="lazy" src="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" data-src="{{ metadata.cloudinary }}/h_32/{{ pub.data.logo }}" alt="{{ pub.data.title }}" width="32" />
          </div>
        {% endif %}
      {% endfor %} 

    <time class="post-list__date post-list__date--{{ theme | stringify | slug }}" datetime="{{ post.data.date|machineDate }}">
        {{ post.data.date|readableDate }}
      </time> 
    </li>
  {% endif %}
{% endfor %}
</ul>
